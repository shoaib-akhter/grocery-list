import React from "react";

// Functional component for the header
function Header() {
    return (
        <header>
            {/* Main Heading */}
            <h1>🍎 Grocery List Manager 🛒</h1>
            {/* Subheading */}
            <p>A simple list for your shopping needs</p>
        </header>
    );
}

// Export the component so it can be used elsewhere
export default Header;